# Claude Code Entry Point

Read [`AGENTS.md`](./AGENTS.md) for operator principles, working rules, and
session reading order.

This file exists so Claude Code auto-loads `AGENTS.md` via its standard
`CLAUDE.md` discovery. The content lives in `AGENTS.md` because that's the
portable convention across agents (Cursor, Codex, Gemini CLI, Antigravity,
Claude Code).
